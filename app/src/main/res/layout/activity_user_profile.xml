<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_gradient"
    android:padding="16dp">

    <!-- Botón de editar -->
    <ImageButton
        android:id="@+id/btn_edit_profile"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="16dp"
        android:background="@null"
        android:contentDescription="@string/editar_perfil"
        android:src="@drawable/ic_edit" />

    <!-- Título del perfil -->
    <TextView
        android:id="@+id/tv_profile_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="@string/perfil"
        android:textColor="#00B0FF"
        android:textSize="24sp"
        android:textStyle="bold" />

    <!-- Imagen del perfil -->
    <ImageView
        android:id="@+id/img_profile"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_below="@+id/tv_profile_title"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:clipToOutline="true"
        android:src="@drawable/ic_default_profile"
        android:background="@drawable/circle_shape"
        android:contentDescription="@string/imagen_del_perfil" />

    <!-- Contenedor de información del perfil -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_below="@+id/img_profile"
        android:layout_marginTop="32dp">

        <!-- Nombre -->
        <TextView
            android:id="@+id/tv_name_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="nombre"
            android:textColor="#00B0FF"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_name_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="pepito_perez"
            android:textColor="#FFFFFF" />

        <!-- Usuario -->
        <TextView
            android:id="@+id/tv_username_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="usuario"
            android:textColor="#00B0FF"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_username_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="elpepitop"
            android:textColor="#FFFFFF" />

        <!-- Correo -->
        <TextView
            android:id="@+id/tv_email_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="correo"
            android:textColor="#00B0FF"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_email_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="pepito_gmail_com"
            android:textColor="#FFFFFF" />

        <!-- Teléfono -->
        <TextView
            android:id="@+id/tv_phone_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/tel_fono"
            android:textColor="#00B0FF"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_phone_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="tel_345_7891239"
            android:textColor="#FFFFFF" />

        <!-- Edad -->
        <TextView
            android:id="@+id/tv_age_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="edad"
            android:textColor="#00B0FF"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_age_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="edad_21"
            android:textColor="#FFFFFF" />

        <!-- Género -->
        <TextView
            android:id="@+id/tv_gender_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="g_nero"
            android:textColor="#00B0FF"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_gender_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="gender_h"
            android:textColor="#FFFFFF" />
    </LinearLayout>

    <!-- Botón de cerrar -->
    <ImageButton
        android:id="@+id/btn_close"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:background="@null"
        android:src="@drawable/close"
        android:contentDescription="@string/cerrar" />

</RelativeLayout>
